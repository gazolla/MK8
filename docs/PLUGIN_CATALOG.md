> **This class no longer exists as a separate file.**
>
> `PluginCatalog.java` was merged into `PluginManager.java`. All directory scanning,
> capability indexing, hot-reload, and `CatalogEntry` definitions now live in `PluginManager`.
>
> See [PLUGIN_MANAGER.md](PLUGIN_MANAGER.md) for the current reference.
